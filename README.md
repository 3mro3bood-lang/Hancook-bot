# هانكوك بوت - Hancock Bot 🐍

بوت واتساب متطور يعتمد على مكتبة Baileys، يحتوي على ألعاب، نظام بنكي، وصلاحيات للمطورين.

## المميزات
- نظام ألعاب (ترتيب، كتابة، تفكيك، أعلام، احزر الإيموجي).
- نظام بنكي (رصيد، زرف، شحن).
- نظام صلاحيات (مالك، نخبة).
- ربط سهل عبر رقم الهاتف (Pairing Code).

## التشغيل على Render
1. ارفع الملفات إلى مستودع GitHub خاص بك.
2. اربط المستودع بـ Render.
3. اختر بيئة التشغيل `Node`.
4. أمر التشغيل: `npm start`.

## ملاحظة
عند التشغيل لأول مرة، سيظهر كود الربط في سجلات (Logs) المنصة، استخدمه لربط البوت برقمك.
